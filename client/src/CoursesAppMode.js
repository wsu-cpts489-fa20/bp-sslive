/* CoursesAppMode: The enumerated type for CoursesAppMode. */

const CoursesMode = {
    COURSELIST: "CourseListMode",
    SEARCH: "SearchMode"
}

Object.freeze(CoursesMode);

export default CoursesMode;